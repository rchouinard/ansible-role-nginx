{{ ansible_managed | comment }}

upstream {{ upstream.name }} {
{% for server in upstream.servers %}
  server  {{ server }};
{% endfor %}
{% if upstream.extra %}
  {{ upstream.extra | indent(width=2) }}
{% endif %}
}
